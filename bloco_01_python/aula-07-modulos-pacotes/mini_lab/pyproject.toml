[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "analise-turma"
version = "1.0.0"

[tool.setuptools.packages.find]
where = ["."]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I"]
