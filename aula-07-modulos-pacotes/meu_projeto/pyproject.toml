[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "analise-turma"
version = "1.0.0"

[tool.setuptools.packages.find]
where = ["."]

[tool.ruff]
line-length = 88

[tool.pytest.ini_options]
testpaths = ["tests"]
